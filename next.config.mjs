  /** @type {import('next').NextConfig} */
  const nextConfig = {
      images: {
        domains: ['cdn.liboqiao.top','github.com','sjc.microlink.io','iad.microlink.io'], // 在这里添加你的图片域名
      },
  };

  export default nextConfig;
